# Railway — NSK5713/stream-api
# https://docs.railway.com/config-as-code

[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
